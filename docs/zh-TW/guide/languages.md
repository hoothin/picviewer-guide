---
title: 語言與多語支持
---

# 語言與多語支持

腳本內置語言包，並支持自動識別與手動切換。

## 語言機制

- `customLang` 設置為 `auto` 時自動檢測瀏覽器語言
- 也可手動指定特定語言

## 自定義語言包

語言包以 `pvcep_lang.js` 為基礎，結構包含：

- 語言列表
- 字段與文案映射
- 語言切換函數

若需要新增翻譯，可基於模板追加新語言條目。

## 建議

- 站點出現文案缺失時，優先檢查 `customLang` 是否匹配。
- 自定義語言時保持 key 完整，避免出現缺失字符串。