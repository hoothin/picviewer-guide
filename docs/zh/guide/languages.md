---
title: 语言与多语支持
---

# 语言与多语支持

脚本内置语言包，并支持自动识别与手动切换。

## 语言机制

- `customLang` 设置为 `auto` 时自动检测浏览器语言
- 也可手动指定特定语言

## 自定义语言包

语言包以 `pvcep_lang.js` 为基础，结构包含：

- 语言列表
- 字段与文案映射
- 语言切换函数

若需要新增翻译，可基于模板追加新语言条目。

## 建议

- 站点出现文案缺失时，优先检查 `customLang` 是否匹配。
- 自定义语言时保持 key 完整，避免出现缺失字符串。
